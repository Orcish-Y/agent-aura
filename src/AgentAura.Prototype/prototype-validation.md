# WPF 观察壳验证记录

请在干净的 Windows 11 虚拟机上执行本协议，并在下表中记录结果。仅当所有核心关卡均通过时，才接受该原型。核心关卡失败会启动同等条件下的 Tauri 对比；这并不自动决定选用 Tauri。

## 核心能力关卡

| 关卡            | 操作步骤                                                                               | 通过门槛                                                                                   | 结果     |
| --------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | -------- |
| 仅托盘生命周期  | 启动壳程序；通过系统托盘显示/隐藏；使用最小化；左键单击托盘图标。                      | 不显示任务栏按钮；显示、隐藏、最小化、还原和聚焦均正常工作。                               | 尚未测量 |
| 窗口行为        | 切换置顶状态，并将鼠标移入/移出窗口。通过标题栏调整大小和拖动窗口。                    | 无边框半透明界面；置顶窗口始终保持最前；标题栏仅在置顶时自动隐藏；未置顶时标题栏保持可见。 | 尚未测量 |
| Agent Item 行为 | 悬停于每个示例 Agent Item，检查其状态提示、控件和长文本。                              | 折叠的一行可展开为四行；每种状态均有形状或文字提示；未直接悬停的长文本以省略号截断，直接悬停该行后才在内部滚动并于两端停顿，无需工具提示。 | 尚未测量 |
| 托盘确认        | 隐藏窗口，开始闪烁托盘图标，然后左键单击托盘图标。                                     | 有一个持续中的提醒可见；还原/聚焦窗口会停止提醒。                                          | 尚未测量 |
| 窗口恢复        | 设置窗口位置和大小，退出后重新启动；然后将其保存的坐标改为不可用显示器上的位置并重启。 | 位置、大小和置顶状态均会保留；屏幕外的位置会回到可见的主显示器。                           | 尚未测量 |
| 框架依赖分发    | 发布框架依赖的 `win-x64`，在有 .NET 10 Windows Desktop Runtime 的 Windows 11 上启动。   | `Start-AgentAura.cmd` 能启动应用；发布目录不携带运行时。                                  | 尚未测量 |
| 缺失运行时恢复  | 在没有 .NET 10 Windows Desktop Runtime 的独立 Windows 11 环境中运行启动器。             | 应用不启动；清楚说明先决条件，只有用户确认才打开 Microsoft 官方下载页。                    | 尚未测量 |

## 成本与稳定性关卡

| 指标       | 方法                                                               | 通过门槛                                               | 结果     |
| ---------- | ------------------------------------------------------------------ | ------------------------------------------------------ | -------- |
| 冷启动     | 在目标虚拟机上进行五次冷启动；计时范围为进程启动至托盘图标可见。   | 中位数不超过 1.5 秒。                                  | 尚未测量 |
| 空闲工作集 | 在无交互的情况下让壳程序保持显示五分钟；使用任务管理器或 PerfMon。 | 不超过 100 MiB。                                       | 尚未测量 |
| 空闲 CPU   | 在同一五分钟区间内测量。                                           | 平均值不超过 1%。                                      | 尚未测量 |
| 包大小     | 测量发布后的 `win-x64` 目录。                                      | 不超过 200 MiB。                                       | 尚未测量 |
| 运行时大小 | 单独测量机器级 .NET 10 Core 与 Windows Desktop 共享运行时目录。     | 单独记录；不计入 Agent Aura 包大小。                  | 尚未测量 |
| 稳定性     | 连续八小时测试托盘显示/隐藏、置顶、悬停展开和重启。                | 不发生崩溃、卡死、托盘图标卡住或无法恢复的屏幕外窗口。 | 尚未测量 |

## 决策记录

**当前建议：**尚未决定。在 Windows 上运行并测量该壳程序之前，不能建议使用 WPF，也无法证明需要进行 Tauri 对比。

如果有任何关卡失败，请在创建对比工单前，在此记录 Windows 版本、机器/虚拟机规格、.NET SDK 版本、准确的复现步骤和观察到的结果。

### 系统信息：
.NET SDK:
 Version:           10.0.301
 Commit:            96856fd726
 Workload version:  10.0.300-manifests.b0c14421
 MSBuild version:   18.6.4+96856fd72

运行时环境:
 OS Name:     Windows
 OS Version:  10.0.26200
 OS Platform: Windows
 RID:         win-x64
 Base Path:   C:\Program Files\dotnet\sdk\10.0.301\

已安装 .NET 工作负载:
没有要显示的已安装工作负载。
已配置为在安装新清单时使用 workload sets。
未安装任何 workload sets。运行 “dotnet workload restore” 以安装工作负载集。

Host:
  Version:      10.0.9
  Architecture: x64
  Commit:       901ca94124

.NET SDKs installed:
  10.0.301 [C:\Program Files\dotnet\sdk]

.NET runtimes installed:
  Microsoft.AspNetCore.App 10.0.9 [C:\Program Files\dotnet\shared\Microsoft.AspNetCore.App]
  Microsoft.NETCore.App 10.0.9 [C:\Program Files\dotnet\shared\Microsoft.NETCore.App]
  Microsoft.WindowsDesktop.App 10.0.9 [C:\Program Files\dotnet\shared\Microsoft.WindowsDesktop.App]

Other architectures found:
  None

Environment variables:
  Not set

global.json file:
  Not found


### 核心体验验证：

#### 1 托盘优先生命周期
功能正常

#### 2 窗口形态和置顶
功能正常

#### 3 Agent Item、状态和文本
未直接悬停的长文本会以省略号截断；直接悬停该文本行后，才会在短暂延迟后于可视区域内往返滚动，并在两端停顿；不显示工具提示。宿主机 WPF 回归测试已通过。

#### 4 Reduced motion
该功能已从原型中移除。详情仍保留淡入效果，托盘闪烁仍可通过 **Flash tray** 触发；长文本不受此设置影响，仅在直接悬停对应文本行时滚动。

#### 5 托盘闪烁与确认
功能正常

### 窗口状态持久化与失屏恢复
功能正常

### 框架依赖发布和运行时先决条件
尚未按 `scripts/check-framework-dependent-startup.ps1` 在两套 Windows 11 环境中重新验证。此前的自包含发布和干净机启动结果不适用于此框架依赖分发路径。

### 性能、包体和稳定性测量
#### 1 冷启动
冷启动正常

#### 2 空闲内存和 CPU
cpu 使用率没问题，但是使用了180M的内存，我们这个软件应该是一个很轻量的软件，不应该占用这么多内润的

#### 3 发布包大小
包170M，我们这个软件应该是一个很轻量的软件

该数值是此前的自包含发布，不适用于当前框架依赖包。重新执行 `scripts/publish-framework-dependent.ps1` 后，记录其输出的包大小，并将“Machine-wide .NET 10 runtime footprint”单独记录在此；共享运行时不计入 Agent Aura 包大小。

#### 4 稳定性
需要测试的时间太长了，只有有时间在测试
