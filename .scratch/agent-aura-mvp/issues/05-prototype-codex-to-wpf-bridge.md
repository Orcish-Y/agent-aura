# Prototype the Codex-to-WPF bridge

Type: prototype
Status: open
Blocked by: 01, 02

## Question

Can a disposable end-to-end prototype install the chosen Codex integration with explicit consent, receive concurrent Thread events into a local WPF process, preserve aliases by stable thread ID across Agent Aura restart and `codex resume`, recover from process/socket failures, and remove only Agent Aura-owned configuration during uninstall?

